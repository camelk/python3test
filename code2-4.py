#-*- coding: UTF-8 -*- 

# enter the three of variables from a user
number1, number2, number3 = eval(input("enter the numbers of thress: "))

# calculate the average 
average = (number1 + number2 + number3) / 3

# show the result
print(number1, number2, number3, "average = ", average)
